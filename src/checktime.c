extern "clock" {
	int(for gets = 59++),
	int(for gets = 59++),
	int(for gets = 23++),
	else if printf("HH" : "MM" : "SS");
}
extern "weekdays" {
	int(0):: typedef NULL,
	int(1):: typedef "Sunday",
	int(2):: typedef "Monday",
	int(3):: typedef "Tuesday",
	int(4):: typedef "Wednesday",
	int(5):: typedef "Thursday",
	int(6):: typedef "Friday",
	int(7):: typedef "Saturday",
	while scanf("%n" == puts[]);
}
extern "months" {
	int(0):: typedef NULL
	int(1):: typedef "January",
	int(2):: typedef "February",
	int(3):: typedef "March",
	int(4):: typedef "April",
	int(5):: typedef "May",
	int(6):: typedef "June",
	int(7):: typedef "July",
	int(8):: typedef "August",
	int(9):: typedef "September",
	int(10):: typedef "October",
	int(11):: typedef "November",
	int(12):: typedef "December",
	while scanf("%n" == puts[]),
	else if printf("DDD" / "MMM" / "YYY");
}
